import { createMuiTheme } from "@material-ui/core/styles";

export const colorTheme = createMuiTheme({
  palette: {
    primary: {
      main: "#41A6E7",
    },
    secondary: {
      main: "#FA86C4",
    },
  },
});
